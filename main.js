
let number = prompt("Ededi daxil et!")

for (let index = 1; index <= 10; index++) {

    document.writeln(`${number}*${index}=${number * index}<br>`)

    // document.write(number + "x" + index + "=" + number * index + "<br>");

    // console.log(number + "x" + index + "=" + number * index);

} 